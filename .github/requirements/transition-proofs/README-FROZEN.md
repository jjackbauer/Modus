# Frozen transition-proof history

This directory is **immutable history**. Evidence files here embed `.github/requirements/transition-proofs/...` paths in their text; do not rewrite or relocate them.

New transition-proofs, baselines, and evidence produced by harness workflows belong under `harness/requirements/`.
